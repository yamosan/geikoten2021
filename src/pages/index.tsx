import type { NextPage } from "next";

import { Layout } from "@/components/layouts/Layout";

const Root: NextPage = () => {
  return (
    <Layout>
      <h1>Hello Next.js!</h1>
    </Layout>
  );
};

export default Root;
